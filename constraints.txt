# CI/test dependency constraints for known transitive compatibility gaps.
# Issue #137 isolates the verified Python 3.12 Bayesian/JAX profile. Keep its
# public API surface on the tested minor lines; the hash-pinned external lock
# fixes all transitive versions for the canonical replay.
arviz==0.23.*
pymc==5.26.*
jax==0.9.*
numpyro==0.21.*
# Ruff 0.13+ expands default lint enforcement for the historical source tree;
# keep CI deterministic until the repository intentionally adopts that rule set.
ruff>=0.8,<0.13
# Issue #133 profile-likelihood adapter is exercised through the optional extra.
iminuit>=2.32,<3
# Issue #134 OpenTURNS adapter is exercised through the optional uncertainty extra.
openturns>=1.27,<2
