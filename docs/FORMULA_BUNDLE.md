# FEM formula bundle

`finite_element_options.contracts.formula_bundle.finite_element_formula_bundle()` emits `formula_bundle.v1` weak-form, basis, and quadrature metadata for downstream artifact rendering.
